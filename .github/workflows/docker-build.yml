name: Build Docker images (disabled)

# Docker builds disabled — using Vercel for deployment
# To build Docker images locally for testing:
#   docker build -t community-portal-backend:latest -f backend/Dockerfile ./backend
#   docker build -t community-portal-frontend:latest -f frontend/Dockerfile ./frontend

