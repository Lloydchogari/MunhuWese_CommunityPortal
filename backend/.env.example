# Database configuration
DATABASE_URL="postgresql://postgres:password@localhost:5432/community_portal?schema=public"
JWT_SECRET="your-super-secret-jwt-key-change-this-for-production"
PORT=4000
CLIENT_URL="http://localhost:3000"
NODE_ENV="development"

# Gmail SMTP Configuration (for sending emails)
GMAIL_USER="your-email@gmail.com"
GMAIL_APP_PASSWORD="your-16-character-app-password"

# Instructions:
# 1. Get Gmail App Password:
#    - Go to https://myaccount.google.com/apppasswords
#    - Select "Mail" and "Windows Computer" (or your device)
#    - Google will generate a 16-character app password
#    - Copy it and paste it as GMAIL_APP_PASSWORD
#
# 2. Update CLIENT_URL if running frontend on different port
